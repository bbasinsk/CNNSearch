import 'package:flutter/material.dart';

class NBAResultWidget extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    var title = new Text("Lebron James");

    return new Expanded(
        child: title,
    );
  }
}